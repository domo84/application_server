# Application Server

Application server for bleeding edge front-end

## Setup

inside `package.json` add:
```json
{
	"application_server": {
		"reload": {
			"room": "admin",
			"host": "0.0.0.0",
			"port": 8010
		}
	}
}
```
